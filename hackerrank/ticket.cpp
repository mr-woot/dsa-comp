#include <bits/stdc++.h>
#define _ ios::sync_with_stdio(0);cin.tie(0);

using namespace std;

int solution(vector<int> ar) {
    
}

// n people, m ticket windws, k destinations
// 

int main() { _
    int np, nt, nd;
    cin >> np;
    cin >> nt;
    cin >> nd;
    

    return 0;
}